# Formulasearch 网站：从这里开始

日常内容更新主要在 `content/` 和 `public/uploads/` 完成；页面组件、schema、脚本和发布门禁仍由开发流程维护。不要把“改内容”理解成可以跳过校验或构建。

## 0. 打开本地内容后台

双击根目录的 [`打开内容后台.bat`](打开内容后台.bat)，浏览器会打开 `http://127.0.0.1:4321/content-studio`。后台使用 daisyUI 统一控制中心、内容编辑、照片管理和发布中心布局；首页、Blog、目录、Partners、Architecture、导航、SEO 与 UI 文案继续由 Keystatic 编辑。保存不会自动提交或部署。

## 1. 改首页的自我介绍

优先在内容后台编辑。对应文件仍是中文 [`content/site/home.json`](content/site/home.json)、英文 [`content/site/home.en.json`](content/site/home.en.json)，邮箱、社交链接和首页图片统一保存在 [`content/site/home.shared.json`](content/site/home.shared.json)。

你可以直接改 `name`、`intro`、`about`、`interest`、`sponsor` 和邮箱等字段。`intro`、`about`、`interest` 是数组，中文和英文的条目数量必须保持一致；修改首页姓名时，中文/英文称呼都必须属于 `content/site/site.json` 的作者名或别名。保存后运行 `npm run content:check` 可先校验内容、i18n 和媒体清单。

## 2. 上传首页图片

在内容后台的“首页共享设置”上传图片，或把图片放进 [`public/uploads/home/`](public/uploads/home/) 并在 `home.shared.json` 填入路径，例如：

```json
{
  "heroImage": "/uploads/home/phil-working-desk.jpg"
}
```

如果不想显示图片，在共享设置中清空图片。上传后还要填写真实宽高，并在中文首页和英文首页分别填写对应的 `heroImageAlt`。

## 3. 更新 Blog

Blog 文章位于 [`content/blog/`](content/blog/)，每个目录的 `index.md` 是唯一内容源。优先在 Keystatic 新建和编辑；旧 txt 已迁移并删除。单独检查可运行：

```bash
npm run blog:check
```

正文图片需要具体 `alt`；替换封面后要运行 `npm run blog:media:prepare` 更新尺寸和 WebP/AVIF 变体。外链平台 ID 使用 `wechat`、`x`、`original`，显示文案不要直接写进文章数据。

## 4. 预览与发布

```bash
npm run content:prepare
npm run content:publish
```

`content:prepare` 生成媒体、检查内容与类型、构建 42 个静态页面并启动本地生产预览，不操作 Git。确认页面和 diff 后再运行 `content:publish`；它只允许 `main`、拒绝无关代码变更，并要求 Vercel 与最终发布确认。

## 网站当前状态

- 首页已完成并可本地预览。
- 项目、写作、资源与关于页的栏目结构已确定，但只会在对应真实内容核验后上线，不会用假内容填充。
- 框架说明见 [`docs/13_正式项目基线.md`](docs/13_正式项目基线.md)。
- 首页的结构和设计规则见 [`docs/14_首页信息架构.md`](docs/14_首页信息架构.md)。

## 如果要本地看网页

开发者只需要运行一次：

```bash
npm run dev
```

然后打开终端显示的本地网址。你保存首页 JSON 后，网页会自动更新。
