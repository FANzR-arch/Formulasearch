# Formulasearch 网站：从这里开始

日常内容更新主要在 `content/` 和 `public/uploads/` 完成；页面组件、schema、脚本和发布门禁仍由开发流程维护。不要把“改内容”理解成可以跳过校验或构建。

## 1. 改首页的自我介绍

打开中文首页数据 [`content/site/home.json`](content/site/home.json)；英文文案对应 [`content/site/home.en.json`](content/site/home.en.json)。

你可以直接改 `name`、`intro`、`about`、`interest`、`sponsor` 和邮箱等字段。`intro`、`about`、`interest` 是数组，中文和英文的条目数量必须保持一致；保存后 `npm run build` 会自动校验 JSON、邮箱、链接和双语段落数量。

## 2. 上传首页图片

把图片拖进 [`public/uploads/home/`](public/uploads/home/)，然后在两个首页 JSON 的 `heroImage` 字段填入图片路径，例如：

```json
{
  "heroImage": "/uploads/home/phil-working-desk.jpg"
}
```

如果不想显示图片，两个文件都保持 `"heroImage": ""` 即可。

## 3. 更新 Blog

Blog 文章位于 [`content/blog/`](content/blog/)，每个日期目录的 `index.md` 是页面内容源；迁移用的标题、摘要、分类和链接文本仍由 [`content/blog/README.md`](content/blog/README.md) 说明。新增或迁移文章后运行：

```bash
npm run blog:migrate
npm run blog:check
npm run blog:images:check
npm run blog:media:check
```

正文图片需要具体 `alt`；替换封面后要运行 `npm run blog:media:prepare` 更新尺寸和 WebP/AVIF 变体。外链平台 ID 使用 `wechat`、`x`、`original`，显示文案不要直接写进文章数据。

## 4. 发布前验证

```bash
npm run check
npm run build
npm run test:smoke
```

其中 `build` 会检查内容 schema、文章状态、图片资源、静态路由、canonical、RSS、sitemap 和危险 URL；`test:smoke` 会启动静态预览并回归导航、语言/主题、归档和文章交互。

## 网站当前状态

- 首页已完成并可本地预览。
- 项目、写作、资源与关于页的栏目结构已确定，但只会在对应真实内容核验后上线，不会用假内容填充。
- 框架说明见 [`docs/13_正式项目基线.md`](docs/13_正式项目基线.md)。
- 首页的结构和设计规则见 [`docs/14_首页信息架构.md`](docs/14_首页信息架构.md)。

## 如果要本地看网页

开发者只需要运行一次：

```bash
npm run dev
```

然后打开终端显示的本地网址。你保存首页 JSON 后，网页会自动更新。
