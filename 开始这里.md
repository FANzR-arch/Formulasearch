# Formulasearch 网站：从这里开始

你不需要改代码。以后只需要做两种事。

## 1. 改首页的自我介绍

打开 [`content/site/home.md`](content/site/home.md)。

你可以直接改名字、简介、当前在做的事、三个工作方向和邮箱。请保留 `## Intro`、`## Work` 这类标题，以及列表开头的 `-`；它们告诉网站每段文字应该出现在哪里。

## 2. 上传首页图片

把图片拖进 [`public/uploads/home/`](public/uploads/home/)，然后在 `content/site/home.md` 顶部填入图片路径，例如：

```text
heroImage: "/uploads/home/phil-working-desk.jpg"
```

如果不想显示图片，保持 `heroImage: ""` 即可。

## 网站当前状态

- 首页已完成并可本地预览。
- 项目、写作、资源与关于页的栏目结构已确定，但只会在对应真实内容核验后上线，不会用假内容填充。
- 框架说明见 [`docs/13_正式项目基线.md`](docs/13_正式项目基线.md)。
- 首页的结构和设计规则见 [`docs/14_首页信息架构.md`](docs/14_首页信息架构.md)。

## 如果要本地看网页

开发者只需要运行一次：

```bash
npm run dev
```

然后打开终端显示的本地网址。你保存 `content/site/home.md` 后，网页会自动更新。
