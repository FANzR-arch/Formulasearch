# Formulasearch 网站：从这里开始

你不需要改代码。以后只需要做两种事。

## 1. 改首页的自我介绍

打开中文首页数据 [`content/site/home.json`](content/site/home.json)；英文文案对应 [`content/site/home.en.json`](content/site/home.en.json)。

你可以直接改 `name`、`intro`、`about`、`interest`、`sponsor` 和邮箱等字段。`intro`、`about`、`interest` 是数组，中文和英文的条目数量必须保持一致；保存后 `npm run build` 会自动校验 JSON、邮箱、链接和双语段落数量。

## 2. 上传首页图片

把图片拖进 [`public/uploads/home/`](public/uploads/home/)，然后在两个首页 JSON 的 `heroImage` 字段填入图片路径，例如：

```json
{
  "heroImage": "/uploads/home/phil-working-desk.jpg"
}
```

如果不想显示图片，两个文件都保持 `"heroImage": ""` 即可。

## 网站当前状态

- 首页已完成并可本地预览。
- 项目、写作、资源与关于页的栏目结构已确定，但只会在对应真实内容核验后上线，不会用假内容填充。
- 框架说明见 [`docs/13_正式项目基线.md`](docs/13_正式项目基线.md)。
- 首页的结构和设计规则见 [`docs/14_首页信息架构.md`](docs/14_首页信息架构.md)。

## 如果要本地看网页

开发者只需要运行一次：

```bash
npm run dev
```

然后打开终端显示的本地网址。你保存首页 JSON 后，网页会自动更新。
