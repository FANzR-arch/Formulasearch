{
  "name": "formulasearch-site",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "astro dev",
    "build": "npm run blog:check && astro check && astro build",
    "preview": "astro preview",
    "check": "astro check",
    "blog:migrate": "node scripts/prepare-blog-content.mjs --write",
    "blog:import": "node scripts/import-blog-bodies.mjs",
    "blog:check": "node scripts/prepare-blog-content.mjs --check",
    "photos:prepare": "node scripts/prepare-photo-archive.mjs"
  },
  "devDependencies": {
    "@astrojs/check": "^0.9.4",
    "@astrojs/markdown-remark": "^7.2.2",
    "@types/node": "^24.0.0",
    "astro": "^7.2.0",
    "sharp": "^0.35.3",
    "typescript": "^5.9.3"
  },
  "dependencies": {
    "@lucide/astro": "^1.31.0"
  },
  "registries": {
    "@react-bits": "https://reactbits.dev/r/{name}.json"
  }
}
