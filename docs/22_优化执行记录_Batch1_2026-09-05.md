# Batch 1A / 1B 执行记录

日期：2026-09-05。基于 `main / d8ac7ed` 的工作区修改；未 commit、push 或部署。

本轮完成图片交付与光标降级，以及后台任务启动、上传反馈和照片写入保护。原始图片、正文、照片清单、依赖及品牌动效均保持原样。以下结果对应本地构建与测试，不代表线上已更新。

## 1. 已完成的修改

| 审查项 | 具体文件 | 修改后的行为 |
| --- | --- | --- |
| F01 首页头像 | [HomeAvatar.astro](D:/00_Formula/03_Coding/00-Formulasearch/src/components/HomeAvatar.astro)、[ThemeImage.astro](D:/00_Formula/03_Coding/00-Formulasearch/src/components/ThemeImage.astro) | Astro 构建生成 160／400／600px WebP；桌面预留 2.7 倍 hover 的清晰度，触屏使用较小图片。保留原裁切、圆角、放大方式。 |
| F03 项目卡片封面部分 | [ProjectCard.astro](D:/00_Formula/03_Coding/00-Formulasearch/src/components/ProjectCard.astro)、ThemeImage | 卡片生成 480／800／1200px WebP，按显示尺寸与 DPR 选择，保留 lazy loading。涵盖最大的 Lab 双主题封面及同一组件中的其他卡片。详情轮播与 Showcase 尚未调整。 |
| F01／F03 主题资源选择 | ThemeImage、[site-shell.js](D:/00_Formula/03_Coding/00-Formulasearch/public/scripts/site-shell.js) | 两套地址先保存在不触发下载的 template 中，内联初始化后只请求当前主题；保存的主题优先于系统主题。切换时更新同一个 img，禁用 JS 时仍显示浅色备用图片。 |
| F07 光标降级 | [site-cursor.js](D:/00_Formula/03_Coding/00-Formulasearch/public/scripts/site-cursor.js)、[icon-system.css](D:/00_Formula/03_Coding/00-Formulasearch/src/components/icon-system/icon-system.css) | 控制器成功定位并显示替代光标后才隐藏原生光标；脚本未加载、失焦、离开窗口或切到触屏时恢复。保留已有 Lucide 图形及减少动态效果下的光标。 |
| F02 Windows 后台任务 | [task-runner.mjs](D:/00_Formula/03_Coding/00-Formulasearch/src/content-studio/task-runner.mjs)、[api.ts](D:/00_Formula/03_Coding/00-Formulasearch/src/content-studio/api.ts) | 用 process.execPath 直接运行原有 content-prepare／content-publish 入口，传递独立参数；记录启动失败、快速退出与错误退出；关闭父进程日志句柄，拒绝同一 Studio 服务的重复任务。原发布确认条件不变。 |
| F10 上传反馈 | [photos.astro](D:/00_Formula/03_Coding/00-Formulasearch/src/content-studio/pages/photos.astro) | await 前保存表单与 FormData；成功后清空文件选择并保留“导入／重复”数量提示。失败保留文件选择，按钮恢复可用。保存与删除的成功提示也不再被 reload 的通用文案覆盖。 |
| F11 照片写入保护 | [photo-manager.mjs](D:/00_Formula/03_Coding/00-Formulasearch/src/content-studio/photo-manager.mjs)、api.ts、photos.astro | 同一 Studio 服务的写入串行执行；GET 返回清单版本，保存携带版本，过期编辑返回 409 并保留输入。临时清单写完后原子替换；导入失败清理本次新增媒体，避免污染编号。上传目录使用唯一名称，请求期间禁用重复操作。 |

图片共用的小组件只负责这一种主题媒体交付，没有引入图片库、动画库、状态库或新的前端框架。源素材仍可继续编辑或重新生成。

## 2. 性能前后对照

前值来自审查报告的窄屏弱网基线；后值为 3 个独立冷 context 的手机模拟。公共条件：390×844、DPR 2、下载 200,000 B/s、延迟 150ms、CPU 4 倍降速，load 后再观察约 3 秒。本轮脚本明确开启触屏／mobile 模拟；前次 JSON 未完整保存这两个选项，因此时间对比作为方向性证据，不视为严格控制全部变量的性能实验。

| 指标 | 审查基线 | 修改后 | 结果 |
| --- | ---: | ---: | --- |
| 首页已观测资源下载 | 3,257,361 B | 141,469 B，三次一致 | 减少约 95.7% |
| 首页头像资源 | 双主题原图合计约 3.12 MB | 当前浅色主题 4,764 B（含响应开销） | 隐藏主题不下载；桌面和更高 DPR 会选择更清晰变体 |
| Projects 已观测资源下载 | 4,391,507 B | 330,103 B，三次一致 | 减少约 92.5% |
| Lab 卡片封面 | 双主题合计约 3.28 MB | 当前浅色主题 55,936 B（含响应开销） | 同一画面无需下载另一套主题 |
| Projects LCP | 4,380ms | 2,108／2,104／2,160ms，中位数 2,108ms | 模拟条件下更早显示主要内容 |
| 首页 LCP | 16,752ms；复测 16,724ms | 三次均未获得有效 entry | **不报 0，不宣称已达到 2.5s**；开场的 opacity／遮罩仍会影响指标采样，确切原因尚未归因 |
| 初始 CLS | 抽样 0 | 六次均为 0 | 仅覆盖观察窗口，不能代替所有交互布局验证 |

资源统计不含主 HTML，也不等于整页所有懒加载资源。不是线上 p75、Lighthouse 成绩或真实手机测量。背景动效依然存在，且本批没有调整首页开场。

可复查：[测量脚本](D:/00_Formula/03_Coding/00-Formulasearch/output/playwright/batch-1a-2026-09-05/measure.mjs)、[原始数据](D:/00_Formula/03_Coding/00-Formulasearch/output/playwright/batch-1a-2026-09-05/performance.json)、[首页窄屏截图](D:/00_Formula/03_Coding/00-Formulasearch/output/playwright/batch-1a-2026-09-05/home-mobile.png)、[Projects 窄屏截图](D:/00_Formula/03_Coding/00-Formulasearch/output/playwright/batch-1a-2026-09-05/projects-mobile.png)。完整页面截图与旧 viewport 截图的画布尺寸不同，勿据此直接做像素差值。

## 3. 验证记录

- Batch 1A 最终版：既有 69 项公开站回归＋7 项新增行为测试，**一次 76/76 通过**（2 workers）。之前 4 workers 的一次执行有 WebGL 交互测试超时；最终整轮通过，没有改动该测试断言或背景逻辑。
- 图片验证覆盖保存主题与系统主题相反的情况、切换后正确资源、隐藏主题没有请求、禁用 JS 的图片尺寸、故障光标降级及失焦／触屏恢复。实际预览检查了深浅头像、hover 放大和项目卡片。发现并修正的头像内部尺寸回归已加入测试。
- Batch 1B：`node scripts/test-photo-studio.mjs` 通过。覆盖并发导入、重复跳过、两个旧版本保存竞争、失败导入回滚、队列恢复、删除后的旧版本拒绝。所有写入都在临时清单和临时图片目录内。
- `node scripts/test-content-studio-tasks.mjs` 通过。使用临时目录中的无副作用脚本，验证 Windows Node 启动、路径空格、参数与环境变量原样传递、重复启动拒绝、快速退出、非零退出与 spawn 失败。没有执行真实 prepare 或 publish。
- 后台页面 **3/3 测试通过**：上传成功与重复提交、上传失败恢复、版本冲突时保留编辑。页面来自真实 Studio 预览；照片 API 使用明确的测试响应拦截，没有上传／修改真实清单。
- Batch 1B 最终 `npm.cmd run build` 通过：111 页，157 文件诊断 0 errors／warnings／hints；内容指纹仍为 `e85ebe42a29755e3`。生产输出检查通过，后台未混入公开构建。`git diff --check` 通过。

后台 UI 测试命令（先在另一个终端运行本地 Studio）：

```powershell
$env:CONTENT_STUDIO_BASE_URL = 'http://127.0.0.1:4330'
node node_modules/@playwright/test/cli.js test tests/content-studio.spec.mjs --workers=1
```

不设置该环境变量时，普通公开站测试会跳过这 3 项后台专用测试。

## 4. 边界与下一批

并发队列保护同一个本地 Studio 服务的请求，版本检查保护不同浏览器标签页。它不是多个 Studio 服务或外部脚本同时改清单的跨进程事务锁；继续使用项目既有的单服务工作方式。任务退出状态依赖启动它的 Studio 进程仍在运行，服务重启后的状态恢复与 F25 轮询属于后续批次。

F09 的离页草稿保护、导入后保留其他尚未保存的编辑仍未完成；本批只保证版本冲突不会覆盖磁盘，不能宣称所有未保存内容都已得到保护。

对照窄屏旧截图，首页身份介绍行原有的裁切仍存在；它不属于本批图片交付修复，后续移动排版应单独处理。原报告中的其他项目仍按小批次推进，尤其 F04 手机文章目录、F06 键盘焦点和 F12 触屏滚动。

本记录保留原审查报告的历史基线，不回写测量结果为“审查时已修复”。
