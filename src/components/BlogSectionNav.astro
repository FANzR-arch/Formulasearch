---
interface Props {
  active: import('../data/blog-navigation').BlogSectionId
}

import LocalizedText from './LocalizedText.astro'
import { blogSectionNavigation } from '../data/blog-navigation'

const { active } = Astro.props
---

<nav class="blog-section-nav" aria-label={blogSectionNavigation.ariaLabel.zh} data-aria-en={blogSectionNavigation.ariaLabel.en} data-aria-zh={blogSectionNavigation.ariaLabel.zh}>
  {blogSectionNavigation.items.map((item) => (
    <a class:list={{ 'is-active': item.id === active }} href={item.href} aria-current={item.id === active ? 'page' : undefined}>
      <LocalizedText {...item.label} />
    </a>
  ))}
</nav>
